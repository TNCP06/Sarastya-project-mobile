name: CI

on:
  pull_request:
    branches: ["main"]

jobs:
  mobile-check:
    name: Mobile Placeholder Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder Step
        run: echo "No tests defined for mobile project yet."
